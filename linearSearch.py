# linear search
def linearSearch(arr , key):
	for i in range(len(0. len(arr))):
		if arr[i] ==  key:
			print "Key is found and is ", arr[i]
		else: 
			i += 1

arr = [5,6,3,9]
key = 3
linearSearch(arr, key)